# DESIGN & ASSETS AUDIT — JAHEEZ

> **Generated:** 2026-05-05 | **Method:** Full asset inventory inspection

---

## Design Reference Material

### 1. ChatGPT-Generated Design Mockups (`design/`)

14 PNG images generated by ChatGPT Image on April 24, 2026. File sizes range from 442KB to 1.06MB.

These appear to be **UI design reference images** showing various screens of the JAHEEZ app:
- 8 images at timestamp `04_12_27/28 PM` — likely one set
- 6 images at timestamp `04_12_47/48 PM` — likely a second iteration

**Status:** These exist as reference images only. Without viewing them, I cannot confirm which screens they depict, but given the naming pattern they were likely generated in a batch to define the visual direction.

**Risk:** The AI-generated designs may not match the current implementation. The code uses a "Light Warm" design system (cream/white backgrounds, red CTAs) which may or may not align with these images.

---

### 2. HTML/CSS Mockups (`html-preview/`)

16 HTML files + 1 CSS file providing interactive browser mockups:

| File | Screen |
|------|--------|
| `index.html` | Landing/index page |
| `splash.html` | Splash screen |
| `welcome.html` | Welcome screen |
| `login.html` | Login screen |
| `register.html` | Registration |
| `otp.html` | OTP verification |
| `home.html` | Home screen |
| `services.html` | Services listing |
| `restaurant-list.html` | Restaurant listing |
| `restaurant-details.html` | Restaurant detail |
| `cart-checkout.html` | Cart and checkout |
| `orders.html` | Orders listing |
| `tracking.html` | Order tracking |
| `profile.html` | User profile |
| `support.html` | Support screen |
| `styles.css` | Shared CSS (13KB) |

**Status:** These appear to be an **older design direction** — a static HTML prototype created before the React Native implementation. The `server.js` at root serves these files on port 5000.

**Risk:** These mockups may represent an outdated design. The actual React Native screens have likely diverged.

---

### 3. HTML Workspace Mockup (`jaheez_workspace/`)

A comprehensive HTML/CSS design system with:
- `index.html` (22KB) — full design showcase
- `theme.css` (10KB) — design tokens in CSS variables
- `components.css` (20KB) — component styles
- `app.js` (11KB) — JavaScript interactions
- `apps/` and `packages/` subdirectories

**Status:** This appears to be a **standalone design system prototype** — possibly created as a reference for the React Native implementation. It may be from the Replit era of development.

---

### 4. Figma/Prototype Documentation

| File | Purpose |
|------|---------|
| `jaheez_figma_screen_inventory.html` | 33KB HTML file — screen inventory from Figma |
| `jaheez_prototype_spec.html` | 71KB HTML file — detailed prototype specification |
| `jaheez_stitch_prompts_full_system.html` | 45KB HTML file — stitch prompts for the full system |

**Status:** These are large reference documents likely exported from a design process. They define the intended screen inventory and specifications.

---

## App Assets

### User App Icons (`user-app/assets/icons/`)

| File | Size | Purpose |
|------|------|---------|
| `home.png` | 336KB | Home tab icon |
| `orders.png` | 391KB | Orders tab icon |
| `cart.png` | 304KB | Cart icon |
| `chat.png` | 383KB | Chat tab icon |
| `favorites.png` | 354KB | Favorites icon |
| `middle.png` | 67KB | FAB center button icon (custom JAHEEZ icon) |

**Assessment:** Tab icons exist but are **very large** for icons (304-391KB each). These are likely high-res PNGs that should be optimized. They're used as tab bar images and the central FAB button.

---

### User App Illustrations (`user-app/assets/illustrations/`)

| File | Size | Purpose |
|------|------|---------|
| `bag_hero.png` | 1.7MB | Hero illustration (delivery bag) |
| `scooter.png` | 1.3MB | Scooter/delivery illustration |
| `scooter2.png` | 1.7MB | Second scooter illustration |
| `support.png` | 1.5MB | Support illustration |
| `food.png` | 95KB | Food category icon |
| `grocery.png` | 58KB | Grocery category icon |
| `pharmacy.png` | 46KB | Pharmacy category icon |
| `parcel.png` | 41KB | Parcel category icon |
| `errand.png` | 49KB | Errand category icon |
| `discount.png` | 55KB | Discount/promo icon |

**Assessment:** These are **3D-style AI-generated illustrations**. The hero images (bag_hero, scooter, support) are very large (1.3-1.7MB) and need optimization for mobile. The category icons are reasonably sized.

---

### Jaheez Icons Folder (`jaheez icons/`)

| Item | Description |
|------|-------------|
| `40c208fd-*.png` | 1.6MB — appears to be a brand/logo image |
| `b7252770-*.png` | 1.6MB — appears to be a brand/logo image |
| `Capture d'écran *.png` | 76KB — screenshot |
| `icon/` | Subdirectory (contents not inspected) |
| `illustrations/` | Subdirectory (contents not inspected) |

**Assessment:** This appears to be a **duplicate/alternate** icon folder, separate from `user-app/assets/`. The two large PNGs are likely logo variations. This folder seems to be from an earlier phase and may contain assets not yet integrated.

---

### Driver App Assets (`driver-app/assets/`)

**EMPTY** — The driver app has no icons, illustrations, or any assets.

**Assessment:** This is a **critical gap**. The driver app needs its own tab icons, brand images, and potentially maps assets.

---

### Attached Assets (`attached_assets/`)

Contains:
- 15 text files with pasted prompts/specs (used to guide AI code generation)
- 1 PNG image (small, 20KB)
- `screenshots/` directory with 2 admin panel screenshots

**Assessment:** These are **development artifacts** — prompts and references used during the AI-assisted coding process. Not app assets.

---

## Asset Organization Assessment

### ⚠️ Issues

1. **Scattered assets**: Icons and illustrations exist in at least 3 different locations (`user-app/assets/`, `jaheez icons/`, `design/`)
2. **No SVG icons**: All icons are PNGs. SVGs would be smaller and scalable.
3. **Oversized images**: Hero illustrations are 1.3-1.7MB — should be compressed to <200KB for mobile
4. **No app icon**: No proper app icon (adaptive icon, splash image) configured beyond the tab bar icons
5. **No font files**: The app uses `@expo-google-fonts/cairo` (downloaded at runtime) — no local font files
6. **Missing driver assets**: Driver app has zero visual assets
7. **No favicon**: Admin panel has no favicon configured
8. **No loading states**: No app store screenshots, no preview images

### ✅ What's Good

- Category icons exist and are reasonably sized (41-95KB)
- Tab bar icons exist for the user app
- The `middle.png` FAB icon is nicely integrated
- Brand illustrations have a consistent 3D style

---

## What's Missing

| Asset Type | Status |
|------------|--------|
| App icon (iOS/Android) | ❌ Missing |
| Splash screen image | ❌ Missing (code-based splash only) |
| Store placeholder images | ❌ Missing (mock data uses Unsplash URLs) |
| Driver app tab icons | ❌ Missing |
| Driver app illustrations | ❌ Missing |
| Admin panel favicon | ❌ Missing |
| App store screenshots | ❌ Missing |
| Logo in various sizes | 🟡 Partial (only in `jaheez icons/`) |
| Loading animation (Lottie) | ❌ Missing |
| Empty state illustrations | ❌ Missing |
| Error state illustrations | ❌ Missing |
| Map markers (custom) | ❌ Missing (MapMarker component exists but may use default) |
| Onboarding slide images | ❌ Missing (slides likely text-only or emoji-based) |
